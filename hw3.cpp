#include <iostream>

using namespace std;

int main()
{
	int array[10]
	cout << "enter 10 intergers not in numerical order" \n<<;
	for (int i=0, i<10; i++)
	{
		cin >> array[i];
	}
	cout << \n;
	cout << "Array is" \n<<;
	for(j = 0, j < 10, j++)
	{
		if (arr[j] > arr[j+1])
		{
			int swap = arr[j];
			arr[j] = arr[j+1];
			arr[j+1] = swap;
		}
		for (int a = 0; a < 1; a++)
		{
			cout << arr[a] << " " \n <<;
		}
	}
}
